package errors.user

/*
import linter.types.Type


/**
 * Represents type incompatibility.
 * This error is the programmers fault.
 */

open class TypeMismatchError(message: String): UserError(message) {

	constructor(expected: Type?, provided: Type?): this("Expected '${expected?.name}' but got '${provided?.name}'.")
}*/
